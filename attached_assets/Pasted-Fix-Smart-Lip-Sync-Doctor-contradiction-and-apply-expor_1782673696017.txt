Fix Smart Lip Sync Doctor contradiction and apply export offset to video, not audio.

Problem:
Smart Lip Sync Doctor shows a contradiction.

It says:
Perfect / no offset needed

But it also says:

* export matches preview: no
* export fine-tune offset: +0.50s
* total export offset: +0.50s
* ffmpeg offset filter: atrim=start=0.500

This is wrong.

Do not change:

* Sync.so result URL
* project audio start
* clip order
* captions
* effects
* overlays
* watermark
* payments
* credits

Only fix:

* Smart Lip Sync Doctor logic
* export lip sync offset direction
* ffmpeg offset filter
* preview vs export comparison

Requirements:

1. If export matches preview = no, the doctor must never say:
   Perfect / no offset needed.

Instead show:
Export mismatch detected.

2. Positive export offset means:
   delay the lip sync video/mouth.

It should not trim/cut the audio with:
atrim=start=0.500

3. For mouth too early:

* keep the audio section fixed
* delay the lip sync video by the offset
* do not move the song audio

Correct behavior:
Mouth too early = video/mouth delayed by +0.10s, +0.20s, +0.50s, etc.

4. Fix ffmpeg logic.

Do not use this for video delay:
atrim=start=0.500

Use a video delay/padding method instead, such as:

* add 0.50s video padding before the lip sync video
* freeze first frame or black pad for the delay
* then trim final video back to the scene duration
* keep scene audio start/end unchanged

5. Smart Doctor should show separate filters:

Audio filter:
should keep original scene audio section, example:
audio section 0:10 → 0:15.04

Video offset filter:
should show video delayed by +0.50s

6. Add status:
   Offset applied to video: yes/no
   Offset applied to audio: yes/no

For mouth too early, expected:
Offset applied to video: yes
Offset applied to audio: no

7. Fix diagnosis logic:

If:

* export matches preview = no
  Then diagnosis must be:
  Export mismatch detected

If:

* mouth is too early
  Then recommendation:
  Delay video/mouth by +0.10s steps

If:

* offset is applied to audio
  Then issue:
  Offset applied to wrong stream

8. Add a safe test:
   Export Lip Sync Scene Offset Test

This should render:

* the lip sync scene only
* correct scene audio section
* lip sync video delayed by total export offset
* no captions/effects/overlays

9. After test, show:

* audio section used
* video delay used
* offset applied to video
* offset applied to audio
* export matches preview
* result URL

Final goal:
Smart Lip Sync Doctor must not say perfect when export does not match preview. For mouth too early, export must delay the lip sync video/mouth, not trim the audio.
