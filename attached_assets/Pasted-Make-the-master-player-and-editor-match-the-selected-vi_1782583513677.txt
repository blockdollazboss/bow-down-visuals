Make the master player and editor match the selected video aspect ratio.

Problem:
Whatever aspect ratio I am editing/exporting, the master player should match that same shape.

If I choose TikTok/Reels/Shorts 1080x1920, the master player should be vertical 9:16.
If I choose YouTube 1920x1080, the master player should be wide 16:9.
If I choose Square 1080x1080, the master player should be square 1:1.
Everything else should follow the same canvas size.

Do not change:
- Export Doctor working pipeline
- captions sync
- effects
- transitions
- watermark logo
- Auto PiP
- Bow Down AI Guide
- Runway generation
- Stripe, credits, or payments

Only fix aspect ratio/canvas sizing.

Requirements:

1. Add project format/aspect ratio settings:

Formats:
- TikTok / Reels / Shorts — 9:16 — 1080x1920
- YouTube / Landscape — 16:9 — 1920x1080
- Square — 1:1 — 1080x1080
- Instagram Portrait — 4:5 — 1080x1350
- Custom

2. Save selected format to the project:

project.aspectRatio
project.exportWidth
project.exportHeight
project.formatName

3. Master player must match the selected aspect ratio.

Examples:
- 9:16 = vertical player
- 16:9 = wide player
- 1:1 = square player
- 4:5 = portrait player

4. Do not stretch videos.

Inside the master player, add fit modes:
- Fill / Crop
- Fit / Letterbox
- Blur Background Fill

Default:
Fill / Crop for short-form vertical videos.

5. Captions must stay inside the selected canvas.

Caption safe area must adjust to:
- 9:16
- 16:9
- 1:1
- 4:5

6. Watermark must stay inside the selected canvas.

Watermark positions must adjust to:
- bottom right
- bottom left
- top right
- top left

7. Overlays must match the selected canvas size.

Rain, sparks, light leaks, waveform, dust, smoke, lens flare, and watermark should all render inside the exact video frame shape.

8. Timeline thumbnails should match the project format too.

If project is vertical, thumbnails should be vertical.
If project is wide, thumbnails should be wide.

9. Export Doctor and normal export must use the same selected format.

If project format is TikTok/Reels/Shorts:
- export 1080x1920

If project format is YouTube:
- export 1920x1080

If project format is Square:
- export 1080x1080

If project format is Instagram Portrait:
- export 1080x1350

10. Add visible status near the master player:

Project Format:
- format name
- aspect ratio
- export size
- fit mode
- master player matches format: yes/no
- export matches format: yes/no

11. Add a Format Settings section where I can change:
- format preset
- width
- height
- fit mode

12. When I change format:
- master player updates immediately
- captions reposition safely
- watermark repositions safely
- overlays resize safely
- timeline thumbnails update
- export settings update

13. Do not reset my clips, captions, effects, or order when changing format.

Final goal:
The whole editor should use one project canvas. The master player, overlays, captions, watermark, timeline, and export should all match the selected aspect ratio and export size.