Fix draft recovery, project saving, and missing video generation step only.

Problem:
When the page refreshes, I cannot recover my draft.
I have to regenerate lyrics and video prompts again, which costs more credits.
Also, after lyrics and video prompts are created, I no longer see where to generate the actual videos/clips.

Do not change Stripe.
Do not change credits pricing.
Do not change payments.
Do not change Artist Profiles.
Do not change Runway API keys.
Do not redesign the dashboard.
Only fix project saving, draft recovery, workflow state, and missing video generation buttons.

Critical requirements:

1. Autosave draft after every major step
    Automatically save draft to Supabase and localStorage after:

* artist selected
* lyrics generated
* song info entered
* video prompt generated
* scenes generated
* captions generated
* Runway clip generated

2. Recover draft must actually work
    If the app says a draft exists, clicking Recover must restore:

* selected workflow
* active artist
* lyrics
* song info
* video treatment
* scene prompts
* generated video prompts
* current workflow step
* generated clip URLs if any
* captions
* audio selection
* effects settings

3. Do not charge credits again for recovered work
    If lyrics or video prompts were already generated and saved in the draft, do not charge credits again just to display them after recovery.
4. Add clear recovery messages
    Show:

* Draft found
* Recovering draft…
* Draft recovered
* Recovery failed: [real error]
* Draft saved

5. Add a manual backup button
    Add:
    Download Draft Backup

This should download a JSON backup of the current project/draft.

6. Fix Save Project button
    Save Project must save:

* project title
* active artist
* lyrics
* song info
* video scenes
* video prompts
* generated clips
* captions
* audio
* effects
* workflow step

Show:

* Saving…
* Saved
* Save failed: [real error]

7. Restore the video generation step
    After lyrics and video prompts/scenes are generated, the user must clearly see the next step:

Create Video Clips

Each scene card should show:

* scene title
* prompt
* edit prompt
* preview prompt
* Create Video Clip button

The page should have a main button:
Create All Video Clips

If Runway is connected, these buttons should call Runway.
If Runway is not ready, show the real reason instead of hiding the button.

8. Workflow step must persist
    If user refreshes on the video prompt/scenes page, the app should reopen to that same step, not restart from the beginning.
9. Generated clips must save immediately
    As soon as a Runway clip finishes, save it immediately to Supabase with:

* user_id
* project_id
* scene_id
* scene prompt
* final prompt sent to Runway
* Runway job id
* video URL
* thumbnail URL if available
* created_at
* status

10. Add My Projects → Drafts
    Show saved drafts with:

* draft name
* last edited time
* workflow type
* Recover button
* Delete button

11. Add My Projects → Generated Clips
    Show generated clips even if the full project save failed:

* clip preview
* scene title
* date generated
* prompt
* open in editor button

Final goal:
Refreshing the page should never force me to regenerate lyrics/prompts and spend credits again.
After prompts are generated, I should always see where to create the actual video clips.