Fix All 12 Clips + Audio export sync to match master player timeline.

Problem:
Export All 12 Clips Only worked.
But Export All 12 Clips + Audio Only is way out of sync.

Do not change:
- working video-only export
- Scene 4/Hook lip sync result
- Scene 4/Hook lip sync offset
- Sync.so
- captions
- effects
- overlays
- watermark
- payments or credits

Only fix:
- all clips + audio timing
- export timeline clock
- audio start alignment
- clip duration alignment
- master player/export timing match

Requirements:

1. Export All 12 Clips + Audio Only must use the same timeline timing as the master player.

Use one source of truth:
saved timeline order + saved clip durations + saved trims + saved lip sync offsets

2. Do not just place the full audio blindly at 0:00 unless that matches the master player.

Add export audio alignment status:
- master player audio start time
- export audio start time
- audio offset
- timeline video duration
- audio duration
- final export duration
- audio/video sync mode used

3. Use real clip durations from the same source as video-only export.

For each clip show:
- scene number
- title
- video source: lip sync/original
- clip duration used in export
- timeline start time
- timeline end time

4. Make audio use the same start and current timeline as master player.

If the master player starts project audio at 0:00, export should start audio at 0:00.
If master player uses an audio offset, export must use that same offset.

5. Add test button:

Export All 12 Clips + Audio Sync Diagnostic

This should not add captions/effects/overlays.
It should export video + audio only and show:
- clip timeline map
- audio offset
- audio source
- export duration
- whether export audio timing matches master player

6. Add a short test button:

Export First 20 Seconds + Audio Sync Test

This helps test sync faster before rendering the full video.

7. If audio is longer than video:
- do not stretch clips
- export should either trim audio to video length or keep audio only if selected
- for this test, default to trim audio to video length

8. If video is longer than audio:
- do not stretch audio
- leave silence after audio ends unless user chooses loop

9. Add status:
Audio Sync Export Check:
- video-only export passed: yes/no
- audio source ready: yes/no
- export using master player timing: yes/no
- audio offset applied: yes/no
- lip sync offsets preserved: yes/no
- final audio/video sync expected: yes/no

10. Do not proceed to captions/effects until this passes.

Final goal:
Export All 12 Clips + Audio Only should match the master player timing. Video-only already works, so only fix the audio alignment layer.