Fix Mouth Early button in Auto AI Lip Sync Timing.

Problem:
The Auto AI Lip Sync Timing section is visible, but the Mouth Early button is not working.

When I click Mouth Early, it should move the lip sync later by +0.05s because the mouth is moving before the audio.

Do not submit a new Sync.so job.
Do not change Sync.so logic.
Do not change the saved lip sync result.
Do not change master player, timeline, captions, effects, overlays, watermark, export, or payments.

Only fix:
- Mouth Early button click behavior
- Mouth Late button click behavior
- offset saving
- master player preview update

Requirements:

1. Mouth Early button behavior:

When clicked:
- increase lipSyncOffsetSeconds by +0.05
- save the new offset to the selected clip
- update current offset display
- update master player immediately
- preview the selected scene with project audio
- do not submit a Sync.so job

Example:
If current offset is +0.40s and I click Mouth Early:
new offset should become +0.45s

2. Mouth Late button behavior:

When clicked:
- decrease lipSyncOffsetSeconds by -0.05
- save the new offset to the selected clip
- update current offset display
- update master player immediately
- preview the selected scene with project audio
- do not submit a Sync.so job

Example:
If current offset is +0.40s and I click Mouth Late:
new offset should become +0.35s

3. Looks Correct button behavior:

When clicked:
- save current offset
- mark timing as reviewed
- show: Timing looks correct

4. Lock This Timing button behavior:

When clicked:
- save current offset permanently to the selected timeline clip
- set clip.useLipSync = true
- show: Lip Sync timing locked
- do not submit a Sync.so job

5. Add visible click feedback:

When I click Mouth Early, show:
Mouth early detected — moving lip sync later to +[offset]s

When I click Mouth Late, show:
Mouth late detected — moving lip sync earlier to +[offset]s

6. Add debug/status inside the Auto AI Lip Sync Timing section:

- selected scene
- button clicked
- previous offset
- new offset
- offset saved: yes/no
- master player updated: yes/no
- no Sync.so job submitted: yes

7. Buttons should not be disabled if:
- selected scene exists
- lipSyncUrl exists
- useLipSync is true

8. If a button is disabled, show the reason:
- no selected scene
- no lip sync result URL
- lip sync result not active

Final goal:
Clicking Mouth Early should immediately nudge the lip sync later by +0.05s, save it, and update the master player without using Sync.so credits.