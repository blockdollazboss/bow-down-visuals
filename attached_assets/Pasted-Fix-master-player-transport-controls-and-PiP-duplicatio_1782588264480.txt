Fix master player transport controls and PiP duplication/disabled setting.

Problem:
The master player transport controls are not fully functional.

Also Auto Picture-in-Picture still works even when I have it disabled.

There are two PiP buttons showing. There should only be one PiP button.

Do not change:
- aspect ratio icon
- fit mode icon
- fullscreen aspect ratio behavior
- timeline layout
- audio waveform layout
- synced captions
- caption styles
- effects
- overlays
- transparent logo watermark
- Export Doctor working pipeline
- Stripe, credits, or payments

Only fix:
- master player transport controls
- transport sync
- Auto PiP disabled behavior
- duplicate PiP buttons

PART 1 — FIX ALL TRANSPORT BUTTONS

Every master player transport button must actually control the same master playback state.

Fix and verify these controls:

- Play / Pause
- Restart
- Rewind 5 seconds
- Rewind 10 seconds
- Fast-forward 5 seconds
- Fast-forward 10 seconds
- Previous Clip
- Next Clip
- Step Back 1 Frame
- Step Forward 1 Frame
- Mute / Unmute
- Volume
- Playback Speed
- Scrub Bar
- Fullscreen
- PiP

Each control must update:
- master player video
- project audio
- timeline playhead
- audio waveform playhead
- active clip
- active caption
- active effects
- active overlays
- current time display

PART 2 — ONE SHARED TRANSPORT STATE

Create one shared master transport state.

Do not let different controls use separate video/audio state.

Use one source of truth:
- currentTime
- duration
- isPlaying
- muted
- volume
- playbackRate
- activeClipIndex
- isFullscreen
- isPiP
- autoPiPEnabled

All buttons, timeline scrubbing, waveform scrubbing, fullscreen controls, and keyboard shortcuts must read/write this same state.

PART 3 — SCRUB BAR FIX

Scrub bar must work in:
- normal master player
- fullscreen mode
- timeline
- audio waveform

When I drag the scrub bar:
- preview pauses while dragging if needed
- currentTime updates live
- video jumps
- audio jumps
- captions update
- waveform playhead updates
- active clip updates
- release continues playback if it was playing before

PART 4 — REWIND / FAST FORWARD FIX

Rewind and fast-forward must clamp correctly.

Rules:
- never go below 0
- never go past total duration
- update video and audio together
- update timeline and waveform together

PART 5 — PREVIOUS / NEXT CLIP FIX

Previous Clip:
- jump to start of previous clip
- if already inside a clip, jump to start of current clip first if needed

Next Clip:
- jump to start of next clip

Both must update:
- active clip highlight
- captions
- effects
- overlays
- timeline playhead

PART 6 — FRAME STEP FIX

Step Back 1 Frame and Step Forward 1 Frame:
- pause playback first
- move exactly one frame
- default fps: 30
- use project fps if available

PART 7 — FIX AUTO PICTURE-IN-PICTURE DISABLED SETTING

Auto PiP must fully respect the user setting.

If Auto PiP is OFF:
- never enter PiP automatically
- do not enter PiP on scroll
- do not enter PiP on tab switch
- do not enter PiP on blur
- do not enter PiP on visibilitychange
- do not enter PiP on pagehide
- do not enter PiP on leaving editor

Manual PiP button can still work if clicked.

If Auto PiP is ON:
- only use the master player video
- only one PiP can open
- respect browser support
- never use old preview players

PART 8 — REMOVE DUPLICATE PiP BUTTON

There should be exactly one PiP button in the master player.

Remove duplicate PiP buttons from:
- old preview player
- timeline player
- fullscreen duplicate controls
- hidden floating player
- any right-side panels

Keep only:
one PiP button connected to the master player.

In fullscreen, do not create a second PiP button.
Use the same master control component.

PART 9 — PiP STATUS AND SAFETY

Add PiP status inside collapsed debug only:

PiP Debug:
- autoPiPEnabled: true/false
- manualPiPAvailable: yes/no
- activePiPElement: master player yes/no
- duplicatePiPButtonsFound: number
- autoPiPBlockedBecauseDisabled: yes/no

Do not show PiP debug in fullscreen.

PART 10 — KEYBOARD SHORTCUTS

Fix shortcuts so they call the same transport actions:

Space = play/pause
Home = restart
Left Arrow = rewind 5 seconds
Right Arrow = fast-forward 5 seconds
Shift + Left = previous clip
Shift + Right = next clip
Comma = step back 1 frame
Period = step forward 1 frame
M = mute/unmute
F = fullscreen
P = manual PiP

Do not trigger shortcuts while typing in inputs/textareas.

PART 11 — TEST BUTTON

Add a collapsed debug-only test:

Test Transport Controls

It should run checks:
- play works
- pause works
- restart works
- rewind works
- fast-forward works
- previous/next clip works
- frame step works
- scrub sync works
- auto PiP disabled respected
- duplicate PiP buttons count equals 1

Final goal:
The master player should have one fully working transport system. All controls must sync video, audio, waveform, captions, timeline, effects, and overlays. Auto PiP must never activate when disabled. There must be exactly one PiP button.