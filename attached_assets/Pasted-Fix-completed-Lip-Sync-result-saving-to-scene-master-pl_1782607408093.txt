Fix completed Lip Sync result saving to scene, master player, timeline, and refresh persistence.

Problem:
The Sync.so lip sync result preview worked, but these are NOT working:

1. Scene 4 does not play with the lip sync result in the master player.
2. Timeline does not show a Lip Sync badge/status on Scene 4.
3. After refresh, Scene 4 does not still use the lip sync result.

Do not submit another Sync.so job.
Do not change Sync.so submit logic.
Do not change billing, API keys, captions, effects, overlays, watermark, Export Doctor, or payments.

Only fix:
- saving the completed lip sync result
- attaching it to the selected scene/clip
- master player using it
- timeline showing it
- refresh persistence

Requirements:

1. When I click Save Result to Scene or Use Result in Master Player, save the completed result URL to the selected timeline clip.

Save to the real timeline clip object:
- clip.lipSyncUrl = completed Sync.so result URL
- clip.lipSyncStatus = success
- clip.lipSyncProvider = sync.so
- clip.lipSyncJobId = existing job ID
- clip.useLipSync = true
- clip.updatedAt

2. Save it to the database/project storage, not just React state.

After save, show:
Lip Sync result saved to Scene 4

If save fails, show the real error.

3. Master player must prefer lip sync URL when available.

For each clip, master player video source priority should be:

if clip.useLipSync === true and clip.lipSyncUrl exists:
  use clip.lipSyncUrl
else:
  use original clip video URL

4. Timeline must show a visible badge on the clip:

LSX
Lip Sync
or
Lip Synced

For Scene 4, show:
Lip Sync: success

5. Clip selector should show lip sync status too.

Scene 4 — Hook should show:
Lip synced

6. Refresh persistence:

After page refresh:
- load clip.lipSyncUrl from saved project/database
- keep clip.useLipSync = true
- master player still uses lipSyncUrl
- timeline badge still shows
- Lip Sync section still shows saved result

7. Add toggle:

Use Lip Sync Result: on/off

If off:
- master player uses original clip
- saved result remains stored

If on:
- master player uses lip synced clip

8. Add repair button:

Attach Completed Result to Selected Scene

This should use the already completed Sync.so result URL and attach it to the currently selected scene without submitting a new job.

9. Do not hide old completed result.

If completed job exists and result URL is available, keep showing:
Preview Lip Sync Result
Use Result in Master Player
Save Result to Scene
Attach Completed Result to Selected Scene

10. Add visible status:

Lip Sync Save Status:
- selected scene
- completed result URL available: yes/no
- saved to clip: yes/no
- useLipSync: yes/no
- master player using lipSyncUrl: yes/no
- timeline badge visible: yes/no
- persisted after refresh: yes/no

Final goal:
The completed Sync.so result should become the actual video source for Scene 4 in the master player, show a Lip Sync badge on the timeline, and still be attached after refreshing the page — without submitting another paid job.