Fix export clip durations so Scene 1 does not play too long.

Problem:
The exported audio starts correctly from the beginning of the song.

The real issue is the first scene takes around 10 seconds, so the audio/video gets out of sync.

The export must use the same clip durations as the master player timeline.

Do not change:
- project audio start
- full song audio source
- lip sync result
- lip sync offset
- captions
- effects
- overlays
- watermark
- payments or credits

Only fix:
- clip duration used in export
- scene timing
- timeline clip duration map
- first 20s audio test
- all clips + audio export

Requirements:

1. Export must use saved timeline clip durations, not raw video file durations.

For each clip, use this priority:
- timelineClip.trimmedDuration
- timelineClip.duration
- saved scene duration from master player timeline
- explicit clip end - start
- raw video metadata only as fallback

2. Scene 1 should not play for 10 seconds unless the master player timeline says Scene 1 is 10 seconds.

3. Add Clip Duration Match status:

For every clip show:
- scene number
- title
- master player duration
- export duration
- raw video duration
- using trimmed duration: yes/no
- matches master player: yes/no

4. If export duration does not match master player duration, block audio export and show:
Clip duration mismatch — export would go out of sync.

5. Add button:
Rebuild Export Timeline From Master Player

This should copy the exact current master player timeline timing into export:
- clip order
- clip start time
- clip end time
- clip duration
- trims
- lip sync offsets

6. First 20s + Audio Test must use the rebuilt master player timeline.

7. All 12 Clips + Audio Only must use the rebuilt master player timeline.

8. Do not stretch clips.
Do not use raw video duration unless no saved timeline duration exists.

9. Add status:
Export Timeline Source:
- master player timeline: yes/no
- saved timeline durations: yes/no
- raw video durations used: yes/no
- all export durations match master player: yes/no

Final goal:
The export should match the master player timing exactly. Scene 1 should not take 10 seconds unless the master player timeline says it should. Audio starts at 0:00, but video clip durations must line up correctly so the whole export stays synced.