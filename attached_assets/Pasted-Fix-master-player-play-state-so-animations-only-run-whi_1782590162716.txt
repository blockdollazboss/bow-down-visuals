Fix master player play state so animations only run while playing.

Problem:
I should be able to press Play on the master player screen.

Animations/overlays should not keep running when the master player is not playing.

Right now some motion/animation keeps running even when the master player is paused or stopped.

Do not change:
- timeline editing
- clips
- captions timing
- audio waveform layout
- watermark
- aspect ratio icon
- fit mode icon
- fullscreen aspect ratio
- Auto PiP
- Export Doctor
- payments or credits

Only fix:
- master player play/pause state
- animation pause/resume behavior
- overlays/effects animation sync
- timeline playhead animation sync

Requirements:

1. Use the master player as the only play-state source.

One state should control everything:
- isPlaying
- currentTime
- isScrubbing
- isPaused
- isEnded

2. When master player is playing:

These can animate:
- video preview
- project audio
- timeline playhead
- waveform playhead
- animated waveform overlay
- rain
- smoke
- sparks
- dust
- light leaks
- lens flare
- caption animations
- transition animations
- effect animations

3. When master player is paused:

Everything must pause:
- project audio pauses
- timeline playhead stops
- waveform playhead stops
- animated waveform overlay freezes
- rain freezes
- smoke freezes
- sparks freeze
- dust freezes
- light leaks freeze
- lens flare freezes
- caption animation freezes
- transition animation freezes
- effect animation freezes

No requestAnimationFrame loop should keep visually moving overlays while paused.

4. When master player is stopped/restarted:

- currentTime goes to 0
- audio goes to 0
- timeline playhead goes to 0
- waveform playhead goes to 0
- overlays reset to start state
- captions reset to first active caption
- effects reset to start state

5. When scrubbing:

While dragging the scrubber:
- pause animation playback
- update visual frame to the scrubbed time
- do not let particles keep moving freely
- do not let waveform animation keep running
- when scrub ends, resume only if it was playing before scrub

6. Overlays must be time-based, not independent infinite animations.

Tie overlay animation frame to master currentTime.

Example:
overlayFrame = masterCurrentTime

Do not use independent CSS infinite animations that keep running when paused.

If CSS animation is used, set:
animation-play-state: paused

when master player is not playing.

7. Fullscreen must follow same rule.

In fullscreen:
- play starts animations
- pause freezes animations
- scrub freezes/resumes correctly
- no debug panels show

8. Auto PiP must follow same rule.

If Auto PiP opens:
- if master player was playing, continue playing
- if master player was paused, stay paused
- do not start animations by entering PiP

9. Add debug below timeline only:

Playback Animation Debug:
- isPlaying
- isScrubbing
- master currentTime
- audio currentTime
- animation loops active: yes/no
- overlays paused when master paused: yes/no
- waveform paused when master paused: yes/no
- captions paused when master paused: yes/no
- effects paused when master paused: yes/no

Do not show this debug in fullscreen.

10. Add debug-only test:

Test Pause Animation Sync

Test should:
- play master player
- confirm overlays animate
- pause master player
- confirm overlays stop moving
- scrub timeline
- confirm overlays update to scrubbed time but do not freely animate
- play again
- confirm overlays resume

Final goal:
The master player controls all motion. If the master player is not playing, no overlay, effect, waveform, caption animation, particle effect, or timeline animation should keep running.