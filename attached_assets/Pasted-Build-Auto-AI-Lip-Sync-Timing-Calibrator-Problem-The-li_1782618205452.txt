Build Auto AI Lip Sync Timing Calibrator.

Problem:
The lip sync works, but the timing is annoying.
Sometimes it is early, sometimes it is late.
The user should not have to keep guessing Earlier/Later manually.

Do not submit a new Sync.so job.
Do not change Sync.so submit logic.
Do not change the completed lip sync result.
Do not change captions, effects, overlays, watermark, export, or payments.

Only add:
- Auto AI lip sync timing calibration
- offset testing
- master player/export offset matching

Requirements:

1. Add one main button:

Auto Fix Lip Sync Timing

Put it near the Scene 4 Lip Sync controls.

2. When clicked, do not call Sync.so.

Use the already completed lip sync result.

3. Auto Fix should test small playback offsets automatically:

Offsets to test:
-0.25s
-0.20s
-0.15s
-0.10s
-0.05s
0.00s
+0.05s
+0.10s
+0.15s
+0.20s
+0.25s

4. For each offset:
- apply the offset to the selected scene
- preview a short 3-second section in the master player
- compare lip sync result timing against project audio timing
- score the offset

5. If true automatic audio/visual scoring is not possible yet, create a guided Auto Review mode:

Show one offset at a time:
- Play Test
- Mouth Early
- Mouth Late
- Looks Correct

When user clicks:
- Mouth Early → app tries later offset
- Mouth Late → app tries earlier offset
- Looks Correct → app saves that offset

6. Add smart direction rules:

If mouth moves before audio:
- increase offset later by +0.05s

If mouth moves after audio:
- decrease offset earlier by -0.05s

7. Add final save:

When best offset is chosen, save:
clip.lipSyncOffsetSeconds = bestOffset
clip.useLipSync = true

Save to project/database.

8. Master player must use the saved offset immediately.

9. Export Doctor must use the same saved offset.

10. Add status:

Auto Lip Sync Timing:
- selected scene
- current offset
- testing offset
- best offset
- saved offset
- master player using offset: yes/no
- export using same offset: yes/no
- no Sync.so job submitted: yes/no

11. Add button:

Lock This Timing

When clicked:
- saves current offset
- locks Scene 4 lip sync timing
- hides repeated timing warnings

12. Add button:

Reset Timing Calibration

Only resets offset to 0.00s.
It must not submit a new Sync.so job.

13. Add warning:

This fixes playback timing only.
No new Sync.so credits will be used.

Final goal:
The user clicks Auto Fix Lip Sync Timing, the app helps find the correct offset, saves it, applies it to the master player, and uses the same timing in Export Doctor — without charging Sync.so again.