Fix Export Doctor lip sync scene selection and duplicate timeline order.

Problem:
Export Doctor is showing the wrong lip sync test scene.

It shows:
Export Scene 5 Lip Sync Test

But the completed lip sync result is for:
Scene 4 — Hook

Also the All Clips Doctor scene order is duplicated:
5, 5, 1, 2, 4, 3, 1, 2, 4, 3, 5, 5

That means Export Doctor is reading duplicate scene/clip data instead of one clean timeline.

Do not change:
- Sync.so lip sync result
- lip sync timing
- master player playback
- captions
- effects
- overlays
- watermark
- payments or credits

Only fix:
- Export Doctor scene selection
- duplicate timeline order
- lip sync export test button label
- export source priority

Requirements:

1. Export Doctor must use the same selected scene as the Lip Sync section.

If Scene 4 — Hook is selected and has lip sync:
show button:
Export Scene 4 Lip Sync Test

Do not hardcode Scene 5.

2. Export Doctor must find lip synced clips by clip ID, not just scene number.

Use:
- clipId
- sceneId
- timeline clip object

Priority:
if clip.useLipSync === true and clip.lipSyncUrl exists:
use lip sync URL
else:
use original clip URL

3. Fix duplicated timeline order.

Export Doctor should use one source of truth:
current saved timeline clips

Do not merge:
- old generated scenes
- old scene arrays
- lip sync selector list
- duplicate timeline copies

4. Deduplicate export timeline by clipId.

If no clipId exists, deduplicate by:
sceneId + sourceUrl + timelineStart

Do not remove real intentional duplicates if they have unique clipIds.

5. Show clean Export Doctor status:

Export Order:
- order source: saved timeline clips
- total clips
- duplicate clips found: yes/no
- duplicate clips removed: count
- final scene order

6. Lip Sync Export Check should show:

- selected lip sync scene: Scene 4 — Hook
- lip sync clipId
- lipSyncUrl exists: yes/no
- useLipSync: yes/no
- export source: lip sync/original

7. Add correct test button:

Export Selected Lip Sync Scene Test

This should export the currently selected lip synced scene, not a hardcoded scene number.

8. Do not run all 12 clips export yet.

Final goal:
Export Doctor should export the actual selected lip synced Scene 4 — Hook and should not show duplicate scene order like 5,5,1,2,4,3,1,2,4,3,5,5.