Fix First 20s + Audio Test stuck loading with no result.

Problem:
When I click:

Export First 20 Seconds + Audio Test

it shows loading, but nothing happens after that.
No result URL appears.
No error appears.
It just hangs.

Do not change:
- lip sync result
- lip sync offset
- Sync.so
- master player
- captions
- effects
- overlays
- watermark
- payments or credits

Only fix:
- First 20s + Audio Test export button
- export progress status
- timeout handling
- real error reporting

Requirements:

1. When I click First 20s + Audio Test, immediately show:

status: running
started at: [time]
step: preparing clips

2. Show each export step:

- preparing clips
- checking clip durations
- rebuilding export timeline
- downloading clips
- validating clips with ffprobe
- preparing audio
- trimming audio from 0:00 to 20s
- running ffmpeg
- uploading result
- done

3. If any step fails, stop and show the real error.

4. Add timeout handling.

If export takes longer than 2 minutes, show:

Export test timed out.
Last completed step: [step]
Last error: [real error if any]

Do not spin forever.

5. Add server-side logs for this route.

Log:
- request received
- selected project id
- clip count
- first 20s clips used
- audio source
- ffmpeg command summary
- ffmpeg stderr if failed
- upload result

6. Make the frontend poll export status if the export is async.

If async:
- start job
- save job id
- poll job status every 3 seconds
- show progress
- show result URL when done

7. If synchronous:
- return JSON only
- never return HTML
- never leave request hanging

8. Add visible debug under First 20s test:

First 20s Export Debug:
- button clicked: yes/no
- export route called: yes/no
- job id
- current step
- elapsed time
- clip durations loaded: yes/no
- audio ready: yes/no
- ffmpeg started: yes/no
- ffmpeg finished: yes/no
- result URL
- last error

9. Fix the actual hang.

Most likely causes to check:
- server route never returns
- ffmpeg process hanging
- waiting for missing clip duration
- waiting for missing audio file
- upload promise never resolves
- frontend not polling result
- result state not updating

Final goal:
Clicking First 20s + Audio Test should either produce a result URL or show the exact step/error. It must not spin forever.