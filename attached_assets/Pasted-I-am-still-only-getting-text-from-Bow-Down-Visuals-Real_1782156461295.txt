I am still only getting text from Bow Down Visuals. Real Runway video generation is not working yet.

Please debug and fully implement the Runway video generation flow.

Current problem:
Make a Music Video only returns text/music video plan.
I do not see a working real video preview from Runway.

Required behavior:
After the music video plan is generated, every scene card must have a visible button:
Generate Runway Clip

When I click Generate Runway Clip, it must call Runway and return a real video preview.

Use this Replit Secret:
RUNWAYML_API_SECRET

Do not use RUNWAY_API_KEY.
Do not hardcode the key.
Do not expose the key in frontend code.
Only use process.env.RUNWAYML_API_SECRET on the backend/server.

Build or fix this backend route:
POST /api/generate-runway-clip

The route should:
1. Receive a scene prompt from the frontend
2. Validate that RUNWAYML_API_SECRET exists
3. Call the Runway API video generation endpoint
4. Create a video task
5. Poll/check the Runway task status until it succeeds or fails
6. Return the final video URL to the frontend
7. If it fails, return the exact Runway error message

Frontend requirements:
1. After a Music Video Plan is generated, parse the Scene-by-Scene Breakdown into scene cards.
2. Each scene card must show:
   - Scene number
   - Scene prompt
   - Negative prompt if available
   - Generate Demo Clip button
   - Generate Runway Clip button
3. When Generate Runway Clip is clicked:
   - show: Generating Runway clip...
   - call /api/generate-runway-clip
   - display the real returned video URL in a video player
   - save the video URL into the scene card data

Add debug output in development mode only:
- confirm if RUNWAYML_API_SECRET exists without printing the key
- show the Runway task id
- show task status while polling
- show final video URL if successful
- show exact error if failed

Important:
Do not add Stripe.
Do not add other video APIs.
Do not generate full music videos yet.
Do not remove the existing text music video plan.
Only add real Runway clip generation for one scene at a time.