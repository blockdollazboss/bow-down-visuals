Fix Video Editor scene loading from saved projects only.

Problem:
My project saved successfully and contains lyrics, video treatment, and scene-by-scene breakdown text.

But when I click Open Video Editor, the editor says:

* Music video plan loaded: NO
* Runway clips loaded: NO
* No scenes loaded yet
* 0 scenes in this project

The saved project has scene prompts inside the project text, but the Video Editor is not parsing/loading them as real scenes.

Do not change Stripe.
Do not change credits.
Do not change payments.
Do not change Artist Profiles.
Do not change Runway API keys.
Do not redesign anything.

Only fix saved project → Video Editor scene loading.

Requirements:

1. When opening a saved Make Song + Video or Make a Music Video project, load the saved lyrics/video plan into the Video Editor.
2. Parse the saved scene-by-scene breakdown into real scene cards.
3. Each scene card should include:

* scene number
* timestamp
* section
* lyric/line
* location
* action
* camera movement
* lighting
* mood
* AI video prompt
* negative prompt

4. If the project text includes artist consistency text, keep it inside each scene prompt.
5. The Project Status should show:
    Music video plan loaded: YES
6. The editor should no longer show:
    No scenes loaded yet
    0 scenes in this project
7. After scenes load, show:
    Create Video Clip
    on each scene card.
8. Add:
    Create All Video Clips
    only after scenes are loaded.
9. Do not charge credits just to load saved scenes into the editor.
10. Add a button:
    Rebuild Scenes From Saved Plan

This should parse the saved project text and create scene cards without charging credits.

11. Save rebuilt scenes back to Supabase so they stay after refresh.
12. Add clear messages:

* Loading saved scenes…
* Scenes loaded
* Could not parse scenes: [real error]
* Rebuild scenes from saved plan

Final goal:
If a saved project contains a scene-by-scene breakdown, the Video Editor must open with those scenes loaded and ready for video clip generation.