Fix Lip Sync audio timing so it uses the exact scene audio segment, not the full song.

Problem:
The lip sync result works, but the mouth does not match the project audio.
The lip sync is way faster than the audio.

This likely happened because the app sent the full project audio/full mix to Sync.so for a short scene clip.

Do not submit a new Sync.so job automatically.
Do not change master player, captions, effects, overlays, watermark, export, or payments.

Only fix Lip Sync audio timing.

Requirements:

1. Lip Sync must use the exact audio segment for the selected scene.

For selected Scene 4:
- find the scene start time on the timeline
- find the scene end time on the timeline
- calculate scene duration
- extract only that audio section from the project audio

Example:
If Scene 4 starts at 0:22 and ends at 0:24:
Send only 0:22–0:24 audio to Sync.so.

Do not send the full song for a 2-second clip.

2. Add Lip Sync Audio Segment status:

Show:
- selected scene
- scene start time
- scene end time
- scene duration
- project audio duration
- extracted audio segment duration
- selected clip video duration
- audio segment matches clip duration: yes/no

3. Before submitting, verify:

- audio segment duration is close to clip duration
- difference is less than 0.25 seconds

If not, show:
Audio segment duration does not match clip duration. Fix timing before submitting.

4. Create a button:

Preview Lip Sync Audio Segment

This should play only the extracted audio segment that will be sent to Sync.so.

5. Create a button:

Validate Lip Sync Timing

It should show:
- clip duration
- audio segment duration
- difference
- safe to submit: yes/no

6. Add optional controls:

Audio segment offset:
- -1.0s
- -0.5s
- 0
- +0.5s
- +1.0s

Fine adjust:
- minus 0.1s
- plus 0.1s

This lets me line up vocals before submitting.

7. Submit should be blocked unless:

- selected clip exists
- selected scene start/end exist
- extracted audio segment exists
- audio segment is playable
- audio segment duration matches clip duration
- user confirms paid Sync.so job

8. When creating the Sync.so job, send:
- selected clip video URL
- extracted scene audio segment URL

Do not send the full project audio unless the selected clip is the full video length.

9. Keep old completed result saved, but mark it:

Lip Sync timing mismatch

Do not overwrite it until a new corrected job succeeds.

10. Add warning before new paid job:

This will submit a new Sync.so job using the corrected scene audio segment.
This may use credits.

Buttons:
Cancel
Submit Corrected Job

Final goal:
Lip Sync should use the exact audio segment for each scene so the mouth timing matches the project audio. The app must validate timing before submitting another paid Sync.so job.