Fix export clip file preparation before FFmpeg.

Problem:
Export still fails with:
FFmpeg failed (exit 254): Error opening input files: No such file or directory

This means FFmpeg is trying to use local clip files that do not exist.

Do not change:

* master player
* captions
* caption sync
* caption styles
* Auto AI effects
* overlays
* Auto PiP
* Bow Down AI Guide
* Runway generation
* Stripe, credits, or payments

Only fix export file preparation.

Required fix:

1. Before FFmpeg runs, export must prepare real local files for every selected scene.

Do not assume old /tmp files still exist.

2. For every selected clip, resolve the real clip source in this order:

* permanent Supabase Storage URL
* saved clip URL
* Runway output URL
* project clip URL

Do not use stale /tmp paths as the source of truth.

3. Add a button in Export tab:

Prepare Export Files

When clicked:

* check all selected clips
* find the saved clip URL
* download each clip into a fresh export temp folder
* verify the local file exists
* verify file size is greater than zero
* run ffprobe on each file
* show results before export

4. Export should not call FFmpeg until all clips say:

Ready for FFmpeg: yes

5. Add a visible table:

Export File Check:

* scene number
* clip URL found: yes/no
* source type
* HTTP/download status
* local path created
* file exists: yes/no
* file size
* duration
* ffprobe valid: yes/no
* ready for FFmpeg: yes/no
* error

6. If a clip URL is missing, show:
   Scene [number] has no saved clip URL. Click Sync Missing Clips or regenerate that clip.

7. If download fails, show:
   Scene [number] clip download failed: [real error]

8. If local file is missing, show:
   Scene [number] local export file was not created.

9. If a clip URL is expired or private:

* create a fresh signed URL if using Supabase Storage
* then download it
* do not pass expired/private URLs directly to FFmpeg

10. Only after all clips are downloaded and verified, run FFmpeg using the fresh local file paths.

11. Use a new temp folder per export:
    export-[projectId]-[timestamp]

12. Add status:

* Export files prepared: yes/no
* Clips ready: 0/5
* FFmpeg started: yes/no
* FFmpeg skipped because files missing: yes/no

Final goal:
The Export tab should prepare and verify all local clip files before FFmpeg starts, so FFmpeg never fails with “No such file or directory.”
