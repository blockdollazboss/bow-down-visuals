The final exported videos are blank/black.

Do not change Runway generation.
Do not change OpenAI text generation.
Do not change Supabase save/load.
Do not add Stripe.
Only debug and fix final video export.

Current issue:
Runway scene clips show correctly in the scene cards, but the final exported video is blank/black.

Fix requirements:

1. Before exporting, verify each Runway clip URL:
- confirm the URL exists
- confirm it can be fetched/downloaded by the backend
- confirm the downloaded file has a video stream
- confirm the downloaded file duration is greater than 0

2. Do not pass remote video URLs directly into FFmpeg unless confirmed supported.
Instead:
- download each Runway video clip to a temporary local file first
- verify each local file
- then combine the local files with FFmpeg

3. Add FFmpeg debug logs in development mode:
- clip URL
- local temp file path
- file size
- ffprobe video stream info
- duration
- final output path
- final output file size
- final ffprobe output

4. Use a safe export format:
- output mp4
- video codec: libx264
- audio codec: aac
- pixel format: yuv420p
- same resolution for all clips before concatenating
- same frame rate for all clips before concatenating

5. If clips have different sizes or formats, normalize them before concat:
- scale/pad all clips to the same resolution
- use 1080x1920 for vertical videos if project platform is 9:16
- use 1920x1080 for YouTube 16:9
- use 1080x1080 for square

6. If uploaded audio exists:
- add it after video concat works
- trim audio to video duration
- do not let audio replace the video stream

7. Add a test export mode:
Export Video Only Test
This should combine only the Runway clips with no audio.
If video-only export works, then add audio separately.

8. Show clear errors on the page:
- Clip could not be downloaded
- Clip has no video stream
- Clip duration is 0
- FFmpeg export failed
- Final file has no video stream

Goal:
The final exported video should show the actual Runway clips, not a blank/black video.