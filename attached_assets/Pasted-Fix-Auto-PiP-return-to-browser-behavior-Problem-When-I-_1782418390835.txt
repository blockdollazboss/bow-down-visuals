Fix Auto PiP return-to-browser behavior.

Problem:
When I leave the browser or switch away, Auto PiP should keep playing.
When I come back to the browser, PiP should return/reconnect to the master player and continue playing from the same time.

Current issue:
PiP may stop playing, disappear, or not reconnect to the master player correctly.

Do not change Stripe.
Do not change credits.
Do not change payments.
Do not change Runway.
Do not change Artist Profiles.
Do not change scene rebuilding.
Do not change caption timing.
Do not add duplicate players.
Do not create a separate PiP player.

Only fix Auto PiP behavior with the master player.

Requirements:

1. Auto PiP must use the master player video element only.

2. When Auto PiP is active and I switch away from the browser:

* keep audio/video playing
* keep currentTime moving
* keep active scene/caption state updated
* do not stop playback
* do not reset to 0

3. When I return to the browser:

* reconnect PiP state to the master player
* keep the same currentTime
* keep the same active scene
* keep the same active caption
* keep the same play/pause state

4. If PiP window closes manually:

* do not break the master player
* master player continues from same currentTime
* Auto PiP status updates to inactive

5. If the browser exits PiP automatically when returning to the tab:

* master player should continue playing in the page
* do not pause unless the user pressed pause

6. Use proper browser events:

* enterpictureinpicture
* leavepictureinpicture
* visibilitychange
* pagehide
* pageshow
* focus
* blur

7. Add Auto PiP state:

* autoPiPEnabled
* pipActive
* wasPlayingBeforePiP
* lastKnownTime
* lastKnownScene
* lastKnownCaption

8. Do not restart playback when returning.

If it was playing at 1:12 in PiP, returning to browser should continue around 1:12, not 0:00.

9. Add debug under master player:
   Auto PiP Debug:

* autoPiPEnabled
* pipActive
* document hidden
* browser focused
* master currentTime
* lastKnownTime
* returned from PiP: yes/no
* playback restored: yes/no
* last PiP error

10. If browser blocks automatic PiP re-entry, show:
    Browser blocked automatic PiP. Click Enable Auto PiP again.

Final goal:
Auto PiP should keep playing when I leave the browser, and when I come back it should return/reconnect to the master player without stopping, disappearing, or restarting.
