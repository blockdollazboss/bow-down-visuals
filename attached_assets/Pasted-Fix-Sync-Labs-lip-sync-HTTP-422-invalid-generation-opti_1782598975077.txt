Fix Sync Labs lip sync HTTP 422 invalid generation option.

Problem:
Lip Sync audio URL check now works.

It shows:
- audio source type: supabase
- audio reachable: yes

But applying lip sync fails with:

Sync Labs submit failed: HTTP 422
Invalid generation options: property synergize does not exist

This means the Sync Labs request payload is sending an invalid field named:
synergize

Do not change:
- signed URL storage check
- project audio detection
- vocal stem upload
- master player
- timeline
- captions
- watermark
- effects
- overlays
- Export Doctor
- payments or credits

Only fix:
- Sync Labs request payload
- provider options mapping
- lip sync error handling

Requirements:

1. Remove the invalid field from the Sync Labs request payload:

synergize

Do not send this field anywhere to Sync Labs.

2. Audit all Sync Labs payload options.

Only send fields supported by the current Sync Labs API.

Remove or map unsupported fields safely.

3. Add provider-specific option mapping.

Do not use generic lip sync options directly in the provider payload.

Example:
App UI options:
- lipSyncStrength
- preserveFaceIdentity
- preserveArtistLook
- audioSource

Provider payload should map only valid Sync Labs fields.

4. Before submitting, log the sanitized payload in server logs only.

Do not expose API keys.

Show in UI:
Sync Labs payload validated: yes/no
Unsupported options removed: [list]

5. If an unsupported option exists, remove it before submit instead of failing.

Unsupported options removed should include:
- synergize

6. Submit should use:
- signed video URL
- signed audio URL
- valid provider model/version
- valid generation settings only

7. Add a button:
Validate Sync Labs Payload

It should check:
- audio URL ready: yes/no
- video URL ready: yes/no
- provider key present: yes/no
- invalid fields found: list
- sanitized payload ready: yes/no

8. Lip sync should not fake success.

If Sync Labs still rejects the request, show:
- HTTP status
- exact provider message
- invalid field if provided
- sanitized payload keys only

9. Save result only after success.

If failed:
clip.lipSyncStatus = failed
clip.lipSyncError = real provider error

Final goal:
Fix the Sync Labs 422 error by removing the invalid “synergize” option and only sending valid Sync Labs payload fields. Then Apply to Scene should submit successfully or show the next real provider error.