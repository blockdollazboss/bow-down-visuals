Stop Lip Sync from wasting Sync.so credits and track jobs correctly.

Problem:
Lip Sync is submitting to Sync.so, but then my app says:

Sync Labs job timed out after 6 minutes.

This may still use my Sync.so paid credits. I do not want the app to keep submitting new jobs and charging me when the app times out.

Do not change:
- master player
- timeline
- captions
- effects
- overlays
- watermark
- export
- payments

Only fix Lip Sync job safety.

Requirements:

1. Do not resubmit the same scene if a Sync.so job already exists.

Before submitting, check:
- clip.lipSyncJobId
- clip.lipSyncStatus
- clip.lipSyncProvider
- clip.lipSyncSubmittedAt

If a job exists and status is processing, show:
Lip Sync job already processing. Checking status instead of submitting again.

2. Save the provider job ID immediately after Sync.so accepts a job.

Save:
- clip.lipSyncJobId
- clip.lipSyncStatus = processing
- clip.lipSyncSubmittedAt
- clip.lipSyncProvider = sync.so

3. Replace the 6-minute timeout failure with polling.

If the job is not done after 6 minutes, do not mark it failed automatically.

Instead show:
Still processing on Sync.so. Check again in a few minutes.

Status should be:
processing

not failed.

4. Add button:
Check Lip Sync Job Status

This should check the existing Sync.so job ID instead of creating a new job.

5. Add button:
Cancel / Stop Tracking Job

This should stop tracking locally only if provider cancel is unavailable.
Do not submit another job.

6. Disable Apply to Scene while a job is processing.

Show:
Processing — do not resubmit

7. Add confirmation before submitting a paid job:

This may use Sync.so credits.
Submit lip sync job?

Buttons:
Cancel
Submit Job

8. Add Apply Safety Status:

- selected scene
- existing job id: yes/no
- job status
- last submitted time
- will submit new paid job: yes/no
- will check existing job: yes/no

9. If Sync.so returns a result later:
- save result URL
- set lipSyncStatus = success
- show preview in master player

10. If Sync.so truly returns failed:
- show real provider failure
- do not retry automatically
- require user to click Retry

Final goal:
Never waste Sync.so credits by resubmitting the same scene. If a job times out locally, keep it as processing and let me check the existing job status instead of starting a new paid job.