Fix Lip Sync result preview so it does not trap the user and add save/use buttons.

Problem:
Lip Sync result is completed.

When I click Preview Lip Sync Result, it opens a window showing the video, but then the screen gets taken over. When I come back and check status again, I can only click Preview. I cannot clearly save or use the result.

Do not submit another Sync.so job.
Do not change Sync.so submit logic.
Do not change audio timing.
Do not change master player, timeline, captions, effects, overlays, watermark, export, or payments.

Only fix:
- completed lip sync result UI
- preview modal/window
- save result button
- use result in master player button

Requirements:

1. Preview Lip Sync Result should open a clean modal, not take over the whole app.

Modal must include:
- video player showing the completed result
- Close button
- Save Result to Scene button
- Use Result in Master Player button

2. Add these buttons directly under the completed result status too:

- Preview Lip Sync Result
- Save Result to Scene
- Use Result in Master Player

Do not hide these buttons after preview.
Do not replace them with only Preview.
Do not put them in debug.
Do not put them below the fold.

3. If the result is completed and result URL is available, always show:

Lip Sync Result Ready:
- Preview
- Save to Scene
- Use in Master Player

4. Save Result to Scene should save:

clip.lipSyncUrl = result URL
clip.lipSyncStatus = success
clip.lipSyncProvider = sync.so
clip.lipSyncJobId = existing job ID
clip.useLipSync = true

Save to project/database, not just temporary UI state.

5. Use Result in Master Player should:
- save the result if not already saved
- set clip.useLipSync = true
- update master player source for the selected scene
- update timeline badge
- not submit a new job

6. Add a toggle:

Use Lip Sync Result: on/off

Default on after saving.

7. After preview closes, the page should still show:
- Check Job Status
- Preview Lip Sync Result
- Save Result to Scene
- Use Result in Master Player

8. Do not show old failed errors when current job is completed.

9. Add status:

- result URL available: yes/no
- saved to scene: yes/no
- master player using result: yes/no
- timeline badge visible: yes/no

Final goal:
Preview should only let me watch the completed lip sync result. It should not trap the screen or hide the save/use buttons. After preview, I should be able to save the result to the scene and use it in the master player without submitting another paid job.