Fix Timeline Preview master clock, audio playback, scene switching, and captions.

Problem:
Timeline Preview is still not working like a real music video preview.

Current issues:

* Captions show on one scene only.
* The scene does not automatically move to the next scene.
* I have to manually click the next scene.
* When I manually switch scenes, the same caption from the previous scene can stay stuck.
* I do not hear the song/audio playing with the captions.
* Captions should change with the song time.
* Scenes should change automatically with the song time.

Do not change Stripe.
Do not change credits.
Do not change payments.
Do not change Runway.
Do not change Artist Profiles.
Do not change scene rebuilding.
Do not redesign the dashboard.
Do not export video.

Only fix Timeline Preview.

Required behavior:

1. Create one master timeline clock.

When I click Preview Timeline:

* start currentTime at 0
* start the selected song/audio at 0
* start Scene 1
* update captions based on currentTime
* update scenes based on currentTime
* continue playing until the end of the timeline

2. Audio must play during timeline preview.

If audio/song is uploaded or selected:

* play audio when Preview Timeline starts
* audio should continue playing while scenes change
* audio should not restart when Scene 2 starts
* audio should not stop after Scene 1
* audio should pause only when Pause Timeline is clicked
* audio should restart only when Restart Timeline is clicked

If no audio is selected, show:
No song audio selected. Timeline preview will play silently.

If audio fails, show:
Audio preview failed: [real error]

3. Scenes must auto-change.

Do not wait for the user to click each scene.

Use timeline time to choose the active scene.

Example:
Scene 1: 0–10 seconds
Scene 2: 10–20 seconds
Scene 3: 20–30 seconds

When currentTime reaches 10 seconds, automatically show Scene 2.
When currentTime reaches 20 seconds, automatically show Scene 3.

4. Captions must auto-change.

At every timeline time update, find the caption where:

caption.startTime <= currentTime < caption.endTime

Show only that caption.

Do not keep the same caption stuck across all scenes.
Do not require the user to manually click captions.
Do not let a caption from Scene 1 stay on Scene 2 unless its timing actually belongs there.

5. If scenes do not have real video clips yet:

* show a black placeholder card
* show current scene title/section
* show current scene number
* keep audio playing
* keep captions changing
* automatically move to the next scene based on time

6. If scenes have real video clips:

* show the selected scene clip
* still use master timeline time to switch scenes
* do not rely only on the video element ending
* switch to the next scene when timeline time reaches the next scene start

7. Add visible timeline status in Live Preview:

* Timeline Preview Active
* Audio loaded: yes/no
* Audio playing: yes/no
* Current time: [seconds]
* Current scene: [number] of [total]
* Current caption: [caption text or none]
* Total scenes: [number]
* Total captions: [number]

8. Add buttons:

* Preview Timeline
* Pause Timeline
* Restart Timeline
* Preview Scene

9. Difference between buttons:
   Preview Scene = only plays selected scene.
   Preview Timeline = plays the full music video timeline with all scenes, audio, and captions.

10. Caption and scene sync:
    Captions should follow the full song time, not the selected scene only.
    Scenes should follow the full song time, not manual clicking.

11. Do not charge credits for previewing.

Final goal:
When I click Preview Timeline, I should hear the song playing, captions should change automatically with the song time, and scenes should automatically switch from Scene 1 to Scene 2 to Scene 3 without me clicking each scene.
