Fix Export Doctor to allow the master player Replit object storage video URLs.

Problem:
Export Doctor finally found the real Scene 1 source URL.

It is using:
scene.demoClipUrl (master player)

The URL starts with:
https://storage.googleapis.com/replit-objstore...

But Export Doctor says:
Scene 1 URL returns video: no
Last error: Scene 1 URL host is not an allowed media source.

This means the export system is blocking the real video source used by the master player.

Do not change:
- master player
- captions
- effects
- overlays
- Auto PiP
- Runway generation
- Stripe, credits, or payments

Only fix Export Doctor / export media URL validation.

Required fix:

1. Allow the exact media source host that the master player is using:
storage.googleapis.com

Specifically allow Replit object storage URLs:
https://storage.googleapis.com/replit-objstore-...

2. Do not block a URL just because it is not Supabase or Runway CDN.

Allowed video sources should include:
- Supabase storage URLs
- Runway CDN/output URLs
- Replit object storage URLs from storage.googleapis.com/replit-objstore
- any HTTPS URL that passes strict video validation

3. Use validation based on the response, not only the hostname.

A URL should be accepted if:
- it starts with https://
- fetch returns response.ok
- content-type includes video/mp4, video/quicktime, application/octet-stream, or video/*
- or ffprobe validates the downloaded file as video

4. For Scene 1, use the master player source:
scene.demoClipUrl

But also save it into a real export-safe field if missing:
scene.generatedClipUrl
scene.videoUrl
or clip.video_url

Do not leave the only working URL inside demoClipUrl forever.

5. Add status:
Scene 1 master player source accepted: yes/no
Scene 1 Replit object storage allowed: yes/no
Scene 1 downloaded: yes/no
Scene 1 ffprobe valid: yes/no

6. Download Scene 1 from the storage.googleapis.com/replit-objstore URL into the export temp folder.

7. After download:
- verify file exists
- verify file size
- run ffprobe
- only then unlock Export Scene 1 Only

8. If content-type is not video but the file downloads and ffprobe says valid video, accept it.

9. Do not run full export yet.

Final goal:
Export Doctor should accept the same Replit object storage video URL that the master player already plays, download Scene 1, pass ffprobe, and unlock Export Scene 1 Only.