Improve Clips layout and add full controls to the master player.

Problem:
The Clips page videos are too wide. They should be smaller and organized in columns.
Also the master player should have built-in controls on every tab.

Do not change Stripe.
Do not change credits.
Do not change payments.
Do not change Runway.
Do not change Artist Profiles.
Do not change scene rebuilding.
Do not break the shared master player.
Do not add duplicate preview players.

Only fix:

* Clips page layout
* master player controls
* PiP/fullscreen controls on the master player

PART 1 — CLIPS PAGE GRID

On the Clips tab, display generated scene clips as a clean grid.

Use:

* 3 columns on desktop
* 2 columns on tablet
* 1 column on mobile

Each clip card should be smaller, not full-width.

Each clip card should show:

* scene number
* scene title
* clip thumbnail/video preview
* clip ready status
* Preview button
* Regenerate button
* Delete button
* Apply / Use In Timeline if needed

When I click Preview on a clip card:

* do not open a separate player
* load that clip into the top master player
* update activeSceneIndex
* update activeScene
* keep using the shared master player

PART 2 — MASTER PLAYER CONTROLS

The one top master player should be visible on every Video Editor tab:

* Clips
* Timeline
* Music Mixer
* Captions
* Effects
* Branding
* Export

The master player should include controls:

* Play
* Pause
* Restart
* Fullscreen
* Picture-in-Picture
* current time
* total duration
* active scene number/title
* active caption text
* audio playing status

PART 3 — PIP

Add a PiP button to the master player.

If browser Picture-in-Picture is supported:

* open the master video in PiP

If PiP is not supported:

* show message:
  Picture-in-Picture is not supported in this browser.

Do not create a separate PiP player.

PART 4 — FULLSCREEN

Add a Fullscreen button to the master player.

Fullscreen should show:

* the video/scene
* caption overlay
* active clip
* same audio timeline
* same play/pause state

Do not use a separate fullscreen player.

PART 5 — NO DUPLICATES

Remove any old preview players from inside the Clips page.

The Clips page should only have:

* clip grid
* clip controls
* top master player

Final goal:
The Video Editor should feel clean. One big master player at the top with Play, Pause, Restart, Fullscreen, and PiP. The Clips page should show smaller video cards in a 3-column grid, and every clip preview should load into the master player.
