Fix broken draft recovery loop on the lyrics/video prompt page.

Problem:
The app keeps saying I have unsaved work and tells me to recover, but when I click Recover, the draft does not restore correctly. It gets stuck on the lyrics/video prompt page. I cannot save the project there, and I have to regenerate lyrics/video prompts again, which costs more credits.

Do not change Stripe.
Do not change credits pricing.
Do not change payments.
Do not change Runway API.
Do not change Artist Profiles.
Do not redesign the dashboard.

Only fix:

* broken recovery loop
* Save Project on the lyrics/video prompt page
* autosave
* draft discard
* restoring the Create Video Clip buttons

Required fixes:

1. Add a safe recovery modal with 3 buttons:

* Recover Draft
* Download Backup JSON
* Discard Draft

2. Recover Draft must restore:

* workflow step
* active artist
* lyrics
* video treatment
* scene prompts
* generated clip URLs
* captions
* audio
* effects
* project title

3. If recovery fails, show the real error:
    Recovery failed: [error]

Do not keep showing the same broken Recover message forever.

4. Add Discard Draft
    If user clicks Discard Draft:

* clear only the broken local draft for this workflow
* do not delete saved Supabase projects
* do not delete generated clips
* let user continue normally

5. Add Download Backup JSON
    This should download the current local draft data before deleting or recovering anything.
6. Fix Save Project on the lyrics/video prompt page
    The Save Project button must work on that page and save:

* lyrics
* video prompt
* scenes
* active artist
* workflow step
* generated clips if any

Show:

* Saving…
* Saved
* Save failed: [real error]

7. Restore video generation buttons
    After lyrics and scene prompts exist, always show:

* Create Video Clip on each scene
* Create All Video Clips
* Open Video Editor

8. Do not charge credits again just to restore saved lyrics or saved video prompts.
9. Save draft to Supabase and localStorage after every step:

* artist selected
* lyrics generated
* video prompt generated
* scene prompts generated
* clip generated

10. Add a debug line in development:
    Current workflow step: [step name]
    Draft source: localStorage or Supabase
    Recovery status: success/fail

Final goal:
The app should never trap me in a broken Recover Draft loop, and I should be able to save my project from the lyrics/video prompt page.