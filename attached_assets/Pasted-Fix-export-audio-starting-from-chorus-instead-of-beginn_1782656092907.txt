Fix export audio starting from chorus instead of beginning.

Problem:
The video starts at the beginning, but the exported audio starts from the chorus.

The audio should start at the very beginning of the song, 0:00, for the full export and first 20s audio test.

Do not change:
- video-only export
- lip sync result
- lip sync offset
- master player video order
- captions
- effects
- overlays
- watermark
- payments or credits

Only fix:
- export audio source start
- audio trimming
- audio offset
- first 20s audio test
- all clips + audio export

Requirements:

1. Export audio must start from project audio 0:00 by default.

For:
- Export First 20s + Audio Test
- Export All 12 Clips + Audio Only

Use:
audio start = 0.00s

Do not start from:
- hook
- chorus
- selected scene audio segment
- lip sync audio segment
- last previewed audio segment
- Scene 4 / Hook audio segment

2. Separate lip sync audio segment from export full song audio.

Lip sync uses:
selected scene audio segment

Full export uses:
full project song starting at 0:00

Do not reuse the lip sync segment as the export audio source.

3. Add clear status:

Export Audio Source:
- using full project audio: yes/no
- using lip sync segment: yes/no
- audio start time: 0.00s
- audio source URL
- audio source label
- audio starts at chorus: yes/no

4. If export is accidentally using the lip sync segment, block export and show:
Wrong audio source selected — export is using lip sync segment instead of full project audio.

5. Fix First 20s + Audio Test.

It should export:
- first 20 seconds of video timeline
- first 20 seconds of full project audio from 0:00

6. Fix All 12 Clips + Audio Only.

It should export:
- all 12 video clips
- full project audio starting at 0:00
- trim audio to video length only after starting from 0:00

7. Add button:

Reset Export Audio To Song Start

When clicked:
- clears accidental audio offset
- clears chorus start
- clears selected scene audio segment
- sets export audio start = 0.00s
- sets export audio source = full project audio

8. Add diagnostic:

Audio Start Diagnostic:
- full song duration
- selected export audio start
- first 5 seconds playable preview
- source is full mix: yes/no
- source is vocal/lip sync segment: yes/no

Final goal:
The export must start the song from the beginning, not from the chorus. Lip sync audio segments should only be used for Sync.so jobs, never as the full export audio track.