Fix duplicate Live Preview players across tabs. Use one shared player only.

Problem:
The Live Preview window on the Timeline tab is doing better, but:

* it does not pause when I press pause
* it keeps playing after pause
* the Live Preview window on the Captions tab is still broken
* each tab seems to have a different Live Preview player
* all tabs should follow one shared preview player/state

Do not change Stripe.
Do not change credits.
Do not change payments.
Do not change Runway.
Do not change Artist Profiles.
Do not change scene rebuilding.
Do not redesign the dashboard.
Do not work on export yet.
Do not work on caption sync yet.

Only fix:

* duplicate Live Preview players
* shared preview state
* pause/play behavior
* one player used across all Video Editor tabs

Required fix:

1. Find every Live Preview component inside the Video Editor tabs:

* Timeline tab
* Captions tab
* Clips tab
* Music Mixer tab
* any other editor tab

2. Stop each tab from having its own separate preview logic.

3. Create one shared preview state at the parent Video Editor level:

* previewMode
* isPlaying
* isPaused
* currentTime
* audioUrl
* audioDuration
* activeSceneIndex
* activeCaptionIndex
* activeScene
* activeCaption
* audioRef

4. Create one shared Live Preview component.

Every tab must render the same shared Live Preview component using the same shared state.

5. The Timeline tab Live Preview and Captions tab Live Preview must show the exact same:

* current time
* active scene
* active caption
* audio playing status
* pause status

6. Fix pause.

When I press Pause:

* set isPlaying = false
* set isPaused = true
* pause the shared audioRef
* stop any timer or interval
* stop scene auto-advance
* stop caption auto-advance
* Live Preview must freeze on the current scene/caption

When I press Play/Resume:

* set isPlaying = true
* set isPaused = false
* resume the same audioRef from the currentTime
* continue scene/caption auto-advance

7. Do not create a separate audio player inside Live Preview.

There should be only one master audioRef for the whole Video Editor preview.

8. The Captions tab must not use its own old broken preview.

Captions tab should use the same shared Live Preview component as Timeline tab.

9. The Timeline tab must not have a private preview that other tabs cannot access.

10. Add visible debug to every Live Preview:

Shared Player Debug:

* shared player connected: yes/no
* tab name
* previewMode
* isPlaying
* isPaused
* currentTime
* audio playing
* activeSceneIndex
* activeCaptionIndex
* using shared audioRef: yes/no
* duplicate player detected: yes/no

11. Final test:
    Start Preview Timeline.
    Go to Timeline tab live preview.
    It should play.

Press Pause.
Live Preview must pause and stay frozen.

Go to Captions tab.
The Live Preview there must show the same paused scene/caption/time.

Press Resume.
Both tabs must follow the same shared player.

Final goal:
The Video Editor should have one shared preview player. Timeline, Captions, Clips, and Music Mixer should not each have separate broken live preview players.
