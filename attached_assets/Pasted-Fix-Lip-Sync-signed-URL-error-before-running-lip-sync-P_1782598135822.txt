Fix Lip Sync signed URL error before running lip sync.

Problem:
Lip Sync fails with:

Failed to sign URL: 500

The Lip Sync audio source shows:
- project audio found: yes
- vocal stem found: no
- using full mix: yes
- audio ready: yes

But when I click Apply to Scene, it fails before creating a lip sync result.

Do not change:
- master player
- timeline
- captions
- watermark
- effects
- overlays
- Export Doctor
- payments or credits

Only fix:
- Lip Sync signed URL creation
- storage file path detection
- Supabase/storage signing permissions
- real error reporting

Requirements:

1. Do not show only “Failed to sign URL: 500.”
Show the real server-side error:
- bucket name
- file path
- storage provider
- auth role used
- Supabase error message
- stack trace in server logs only

2. Verify the audio file path before signing.

Check:
- project audio URL exists
- audio file path is parsed correctly
- bucket name is correct
- file exists in storage
- service role key is available server-side
- signed URL expiration is valid

3. If the audio URL is already public, do not try to sign it again.

Rules:
- If URL starts with https:// and is publicly reachable, use it directly.
- If it is a Supabase storage path, sign it server-side.
- If it is Replit/object storage URL, use the correct storage access method.
- If it is a blob URL or temporary browser URL, upload it to permanent storage first.

4. Signed URL creation must run server-side only.

Do not expose:
- SUPABASE_SERVICE_ROLE_KEY
- storage secrets
- API keys

5. Add Lip Sync Storage Check button.

Button:
Check Lip Sync Audio URL

It should show:
- project audio found: yes/no
- source type: public URL / Supabase path / Replit storage / blob / unknown
- bucket
- file path
- file exists: yes/no
- signed URL created: yes/no
- signed URL playable: yes/no
- last error

6. Fix Apply to Scene so it first runs the storage check.

If storage check fails, stop and show exact reason.
If storage check passes, continue to lip sync provider.

7. Do the same for selected video clip.

Before lip sync, verify:
- selected clip video exists
- selected clip URL/path is valid
- clip URL can be signed or used directly
- clip URL is playable

8. Add status:

Lip Sync Input Check:
- audio ready: yes/no
- audio signed URL ready: yes/no
- clip video ready: yes/no
- clip signed URL ready: yes/no
- provider connected: yes/no
- ready to submit: yes/no

9. Do not fake success.

If provider is missing, show:
Lip Sync provider not connected yet.

If storage signing fails, show:
Storage signing failed: [real reason]

Final goal:
Fix the “Failed to sign URL: 500” problem by correctly detecting the audio/clip storage source, signing only when needed, and showing the real error. Lip Sync should not run until audio and clip URLs are valid and playable.