Fix export file download/write bug before FFmpeg.

Current error:
Export failed:
ENOENT: no such file or directory, stat /tmp/export.../clip-scene-1.mp4

This means the export code creates a local file path like:
clip-scene-1.mp4

But the file is not actually created before the app tries to stat it or send it to FFmpeg.

Do not change:

* master player
* captions
* caption sync
* caption styles
* Auto AI effects
* overlays
* Auto PiP
* Bow Down AI Guide
* Runway generation
* Stripe, credits, or payments

Only fix export file preparation.

Required fix:

1. Find the export code that creates paths like:
   clip-scene-1.mp4
   clip-scene-2.mp4
   clip-scene-3.mp4

2. Before calling fs.stat, ffprobe, or FFmpeg, the code must actually download/copy the clip video into that exact path.

3. For every selected scene:

* get the real saved clip URL from the project/database
* confirm the URL is not empty
* download the video file
* await the full download/write stream
* only after the write finishes, call fs.stat
* only after fs.stat succeeds, run ffprobe
* only after ffprobe succeeds, send it to FFmpeg

4. Fix async bug:
   Do not start stat/ffprobe/FFmpeg before the file write stream is finished.

Use finished/write promise or await pipeline.

5. If the clip source is a remote URL:

* fetch/download it into the local export folder
* save it as clip-scene-[number].mp4

6. If the clip source is Supabase Storage:

* create a fresh signed URL if needed
* download it into the local export folder

7. If the clip source is already a local path:

* verify it exists
* copy it into the fresh export folder

8. Add clear logs/status for each scene:

* scene number
* source URL/path
* local path
* download started
* download finished
* file exists after download: yes/no
* file size
* ffprobe valid: yes/no

9. If download does not create the file, stop and show:
   Scene [number] failed to download into local export folder.

10. Do not show “Ready for FFmpeg” unless:

* local file exists
* file size is greater than 0
* ffprobe passes

11. Do not run FFmpeg if any local file is missing.

12. Add a test button:
    Prepare Export Files Only

This button should download/copy all selected clips and show whether each local file exists before FFmpeg runs.

Final goal:
The file /tmp/export.../clip-scene-1.mp4 must physically exist before stat, ffprobe, or FFmpeg runs. Export should not fail with ENOENT.
