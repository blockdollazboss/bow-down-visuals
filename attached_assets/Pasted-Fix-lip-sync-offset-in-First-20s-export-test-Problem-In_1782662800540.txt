Fix lip sync offset in First 20s export test.

Problem:
In the 20-second export test, the mouth is too early.

This means the exported lip sync video/mouth starts before the audio, even though the master player timing was already corrected before.

Do not change:
- full project audio starting at 0:00
- clip order
- Scene 12 repair
- captions
- effects
- overlays
- watermark
- payments

Only fix:
- lip sync offset application in First 20s + Audio Test
- export timing for the lip synced Hook scene
- master player vs export lip sync offset match

Requirements:

1. Export First 20s + Audio Test must apply the same lip sync offset as the master player.

2. If the mouth is too early, delay the lip sync video/mouth in export.

Use the saved master player lip sync offset.
Do not apply the offset backwards.

3. Add visible status:

Lip Sync Export Offset Check:
- scene number
- scene title
- saved master offset
- export offset
- offset direction
- mouth too early fix applied: yes/no
- export matches master: yes/no

4. Make sure the offset is applied only once.

Do not double apply it.
Do not ignore it.
Do not reverse it.

5. For mouth too early:
- delay the lip sync video by the offset
- or add lead padding before the lip sync clip
- preserve audio timing

6. Add a small manual export-only adjustment:

Export Lip Sync Fine Tune:
- Mouth Too Early button = delay video/mouth by +0.10s
- Mouth Too Late button = move video/mouth earlier by -0.10s
- Reset Export Lip Sync Offset
- Save Export Lip Sync Offset

7. The export offset should default to the saved master player offset first.
Manual export fine tune should only add a small extra offset on top if needed.

8. Add test button:

Export First 20s Lip Sync Offset Test

This should render only the first 20 seconds with:
- full song audio from 0:00
- current video timeline
- saved lip sync offset
- export fine tune offset if any

9. Show final:
- master offset
- export fine tune offset
- total export offset
- result URL

Final goal:
The 20-second export test lip sync must match the corrected master player timing. Since the mouth is too early in export, the export needs to delay the lip sync video/mouth, not change the audio start.