Fix export audio selection to use the same audio as the master player.

Problem:
The master player has audio working, but the Export tab says:
No audio selected
Export Final Video — 5 clips, no audio

That is wrong.

Do not change Stripe.
Do not change credits.
Do not change payments.
Do not change Runway.
Do not change Artist Profiles.
Do not change scene rebuilding.
Do not change caption timing.
Do not change caption styles.
Do not change the master player layout.
Do not add duplicate players.

Only fix export audio detection.

Requirements:

1. Export must use the same project audio source as the master player.

Use the saved project audio:

* project.video_audio_url
* project.video_audio_name
* project.video_audio_duration
* project.video_audio_source

or whatever existing fields the master player is using.

2. If the master player can play the song, Export must detect that same song.

3. Export tab should show:
   Audio selected: yes
   Audio name: [file name]
   Audio duration: [duration]
   Using master/project audio: yes

4. Remove the wrong message:
   No audio selected

when the master player/project has audio.

5. Export button text should update from:
   Export Final Video — 5 clips, no audio

to:
Export Final Video — 5 clips + audio

6. The exported video must include:

* all 5 clips in timeline order
* selected song audio
* synced captions
* selected caption style
* 9:16 vertical format

7. If audio is missing, show the real reason:

* no project.video_audio_url found
* audio file failed to load
* audio duration missing
* audio not saved to project

8. Add debug in Export tab:
   Export Audio Debug:

* project audio URL found: yes/no
* master player audio URL found: yes/no
* export audio URL found: yes/no
* audio duration
* audio file reachable: yes/no
* last audio error

9. Do not charge credits just to detect audio.

Final goal:
If the master player has song audio, Export must use that same song audio. The export page should not say “no audio” when audio is already working in the master player.
