Rebuild Video Editor preview layout with one big master player at the top.

Problem:
The Video Editor has too many Live Preview players.
Each tab has its own preview/player, which causes bugs and confusion.

I want one big master Live Preview player at the top of the Video Editor that follows all tabs.

Do not change Stripe.
Do not change credits.
Do not change payments.
Do not change Runway.
Do not change Artist Profiles.
Do not change scene rebuilding.
Do not change export logic yet.

Only fix the Video Editor preview layout and player structure.

Required layout:

1. Move the shared Live Preview player to the top of the Video Editor page.

Place it under:

* project title
* project status
* active artist bar

Place it above:

* Clips
* Timeline
* Music Mixer
* Captions
* Effects
* Branding
* Export tabs

2. Make this top player large and clear.

It should show:

* video/scene preview
* current time / total duration
* play/pause button
* restart button
* active scene number/title
* active caption text
* audio playing status
* shared player debug collapsed by default

3. This must be the only player in the entire Video Editor.

Remove all duplicate players from:

* Clips tab
* Timeline tab
* Music Mixer tab
* Captions tab
* Effects tab
* Branding tab
* Export tab

4. Tabs should not create their own preview players anymore.

Tabs should only control the master player.

Examples:

* Clips tab can select a clip, but it updates the top master player.
* Timeline tab can play/pause, but it controls the top master player.
* Music Mixer tab can set the song, but it updates the top master player.
* Captions tab can edit captions, but captions show in the top master player.
* Effects tab can preview effects, but effects show in the top master player.

5. Use one shared preview state at the Video Editor parent level:

* previewMode
* isPlaying
* isPaused
* currentTime
* audioUrl
* audioDuration
* activeSceneIndex
* activeCaptionIndex
* activeScene
* activeCaption
* audioRef

6. Pause must pause everything.

When pause is clicked:

* pause audio
* stop scene changes
* stop caption changes
* freeze the top player

7. Resume must continue everything from the same time.

8. Switching tabs must not reset the player.

If I start preview on Timeline and switch to Captions, the top player should stay at the same time, scene, caption, and pause/play state.

9. Add a visible status:
   Master Preview Active
   Duplicate Players Removed: yes
   Shared State Connected: yes

Final goal:
The Video Editor should have one big master preview player at the top that all tabs follow. No tab should have its own separate video player.
