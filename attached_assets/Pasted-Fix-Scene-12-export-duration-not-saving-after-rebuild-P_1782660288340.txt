Fix Scene 12 export duration not saving after rebuild.

Problem:
Rebuild Export Timeline From Master Player did not fix the mismatch.

Scene 12 still exports as 0.5s even though the master player timeline says Scene 12 is 2.0s.

Do not change:
- audio start
- full project audio
- lip sync result
- lip sync offset
- other scene durations that already match
- captions
- effects
- overlays
- watermark
- payments

Only fix:
- Scene 12 export duration
- rebuild timeline save/apply logic
- last clip trimming/export logic

Requirements:

1. Rebuild Export Timeline From Master Player must update Scene 12 export duration from 0.5s to 2.0s.

2. Check for a last-clip bug.

Common causes:
- final clip duration calculated as totalDuration - clipStart and totalDuration is wrong
- final clip forced to leftover duration
- final clip trimmed by first 20s test logic
- final clip using old downloaded temp file duration
- final clip export cache not cleared
- timeline rebuild skips the last item
- off-by-one array issue
- scene 12 still using stale raw/trim metadata

3. On rebuild, clear stale export cache for Scene 12:
- local temp clip
- duration cache
- ffprobe cache
- old exportTimeline entry
- old downloaded clip metadata

4. Force Scene 12 export timeline entry to use:

start = 24.0s
end = 26.0s
duration = 2.0s

Use the actual master player values if they differ, but do not use 0.5s.

5. Add visible Scene 12 debug:

Scene 12 Debug:
- master start
- master end
- master duration
- old export duration
- new export duration after rebuild
- cache cleared: yes/no
- saved to export timeline: yes/no
- ffmpeg trim duration
- matches master: yes/no

6. If Scene 12 raw video is only 0.5s, do not silently export 0.5s.

Show:
Scene 12 source video is shorter than master duration.

Then either:
- loop/freeze last frame to reach 2.0s for export test
or
- block export and explain source clip is too short

For now, use freeze last frame to pad Scene 12 to 2.0s so the timeline stays synced.

7. Add button:

Repair Scene 12 Duration

This should:
- clear Scene 12 export cache
- set export duration to master duration
- pad/freeze if raw video is shorter
- save export timeline
- refresh comparison table

8. After repair, the table must show:

Scene 12
Master: 2.0s
Export: 2.0s
Match: ✓

9. Do not allow First 20s + Audio Test or full export until Scene 12 matches.

Final goal:
Fix the last clip duration bug. Scene 12 must export as 2.0s to match the master player, not 0.5s.