Hard stop export until real clip files exist. Show exact source URL and local file result.

Current problem:
Export still fails with:
FFmpeg failed exit 254
Error opening input files: No such file or directory

This has happened multiple times. The export code is still sending FFmpeg a local file path that does not exist.

Do not change:
- master player
- captions
- caption sync
- effects
- overlays
- Auto PiP
- Runway generation
- Stripe
- credits
- payments

Only fix export file preparation and add proof before FFmpeg runs.

HARD RULE:
FFmpeg must not run unless every selected clip has a real local file that exists on disk.

Required fix:

1. Add a visible button:
Debug Export Sources

This button must not run FFmpeg.

2. When clicked, show a table for all 5 selected clips with these exact fields:

- Scene number
- Scene title
- Clip database ID
- Provider
- Approved
- Selected
- Actual source URL field name
- Actual source URL value
- Source URL starts with http: yes/no
- Source URL downloadable: yes/no
- Download HTTP status
- Download content-type
- Local export path
- File written: yes/no
- File exists after write: yes/no
- File size bytes
- FFprobe valid: yes/no
- Ready for FFmpeg: yes/no
- Error

3. Do not show only a checkmark under URL.

I need to see the actual source URL field name and actual URL value being used.

4. If the table says URL checkmark but Actual source URL value is missing, show:
URL checkmark was false. No downloadable URL found.

5. For each clip, find the real video URL by checking all possible fields:

clip.url
clip.video_url
clip.videoUrl
clip.output_url
clip.outputUrl
clip.asset_url
clip.assetUrl
clip.runway_url
clip.runwayUrl
clip.storage_url
clip.storageUrl
scene.clip_url
scene.clipUrl
scene.video_url
scene.videoUrl
scene.runway_output_url
scene.runwayOutputUrl
scene.generated_clip_url
scene.generatedClipUrl

6. Once a real URL is found:
- download it with fetch
- confirm response.ok
- confirm content-type is video/mp4 or video/*
- write the file to local export folder
- await the full write
- then run fs.stat
- then run ffprobe

7. Fix the async issue.

Do not call fs.stat, ffprobe, or FFmpeg until the file write is fully finished.

8. Add backend logs:

EXPORT DEBUG START
Scene 1 found source field:
Scene 1 source URL:
Scene 1 download status:
Scene 1 content-type:
Scene 1 local path:
Scene 1 bytes written:
Scene 1 file exists:
Scene 1 ffprobe valid:
EXPORT DEBUG END

9. Disable all export buttons until Debug Export Sources says every clip is:
Ready for FFmpeg: yes

10. If any clip fails, show the exact failing scene and reason.

11. Add a separate button:
Prepare Export Files Only

This must:
- download the real URLs
- write local files
- verify files
- not run FFmpeg

12. Final rule:
No FFmpeg command should start if any selected clip has:
File exists after write: no
or
FFprobe valid: no

Final goal:
I need proof that Scene 1 through Scene 5 have real local files before FFmpeg runs. Stop hiding the actual URL and stop starting FFmpeg with missing files.