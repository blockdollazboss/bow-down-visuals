Make Export Doctor effects match the master player exactly.

Problem:
Export Doctor says effects export connected, but the exported test video does not look the same as the master player.

In the master player I can see:
- Black & White
- Luxury Gold
- caption style
- AI transition preview state

But the exported effects test looks different and weaker. It looks like only one basic effect was applied, not the full master player effect stack.

Also Export Doctor says:
Transitions visible in preview but not connected to export yet.

Do not change:
- master player preview behavior
- synced captions
- audio export
- clip download/export doctor source detection
- Auto PiP
- Bow Down AI Guide
- Runway generation
- Stripe
- credits
- payments

Only fix export rendering so it matches the master player.

Requirements:

1. Export Doctor must read the exact same effect stack used by the master player.

Do not use a separate simplified export effect list.

Export should use:
- selectedEffects
- activeEffects
- captionStyle
- colorGrade
- overlays if enabled later
- structured transitions when applied
- AI edit plan effects
- per-scene effects
- global effects

2. Add an Effect Stack Comparison panel:

Master Player Effect Stack:
- list every active effect
- effect name
- type
- intensity
- opacity
- blend mode
- start time
- end time
- scene scope/global scope

Export Effect Stack:
- list every effect being sent to FFmpeg/export renderer
- effect name
- type
- intensity
- opacity
- blend mode
- start time
- end time
- scene scope/global scope

Add status:
Master/export effect stack match: yes/no

3. If master player has Black & White + Luxury Gold, export must show:
- Black & White applied: yes
- Luxury Gold applied: yes

4. Luxury Gold must be a real export filter, not just a CSS preview.

Export-safe Luxury Gold should use FFmpeg color filters such as:
- warm color balance
- increased contrast
- gold/yellow highlights
- slightly reduced blue/cool tones
- cinematic saturation

5. Black & White must be export-safe too.

If Black & White and Luxury Gold conflict, show:
Effect conflict detected: Black & White reduces color from Luxury Gold.

Then let user choose:
- Black & White only
- Luxury Gold only
- Blend both

6. Add a button:
Render 3-Second Effect Match Test

This exports only the current master player time range for 3 seconds using the exact active effects.

7. Add a visual verification status:
Effect Match Test:
- current master time
- active scene
- master effects found
- export effects applied
- output created
- effect stack matched: yes/no

8. Transitions must be connected next.

If transitions are already applied in the master player, connect them to Export Doctor effects export.

Supported export transitions:
- Cut
- Crossfade
- Fade to Black
- Whip Pan
- Zoom Transition
- Flash Cut

If a transition cannot export yet, mark exactly which one is unsupported.

9. Do not claim “effects export connected: yes” unless the exported file uses the same effect stack as the master player.

10. Preserve:
- all 5 clips
- audio
- synced captions
- caption style

Final goal:
The Export Doctor effects output should visually match the master player. If the master player shows Black & White + Luxury Gold, the exported test must show the same final look, or clearly explain which effect cannot be exported and why.