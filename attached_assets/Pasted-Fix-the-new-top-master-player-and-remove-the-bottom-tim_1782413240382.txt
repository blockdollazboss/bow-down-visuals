Fix the new top master player and remove the bottom timeline preview.

Problem:
The new big master player is at the top, but it is black/empty.
The actual video preview is still showing underneath the tabs on the Timeline page.

That means the top master player is not connected to the active clip/scene/caption yet, and the old bottom timeline preview still exists.

Do not change Stripe.
Do not change credits.
Do not change payments.
Do not change Runway.
Do not change Artist Profiles.
Do not change scene rebuilding.
Do not change export logic.

Only fix the master preview layout.

Required fix:

1. The big top master player must show the actual active video/scene.

It should display:

* active generated clip if the active scene has a clip
* black placeholder only if no clip exists
* active scene number/title
* active caption overlay
* current time / total duration
* play/pause controls

2. Connect the top master player to the same working preview source that is currently showing underneath the tabs.

The video currently showing in the lower Timeline preview should appear in the top master player instead.

3. Remove the bottom preview player under the Timeline tab.

There should not be a second large preview below the tabs.

4. Remove duplicate preview areas from every tab.

Tabs should only show editing controls:

* Clips list
* Timeline controls
* Music tools
* Captions tools
* Effects tools
* Branding tools
* Export tools

5. The top master player should stay visible and working no matter which tab is selected.

6. When I press Play on the top master player:

* audio plays
* scenes advance
* captions change
* active clip shows in the top player

7. When I press Pause:

* audio pauses
* scenes stop
* captions stop
* top player freezes

8. Add status under the top player:
   Master Player Connected: yes
   Duplicate Bottom Preview Removed: yes
   Active Clip Loaded: yes/no
   Active Caption Loaded: yes/no

Final goal:
Only one big top master player should exist. It must show the actual video/caption/audio preview. The old lower timeline preview should be removed.
