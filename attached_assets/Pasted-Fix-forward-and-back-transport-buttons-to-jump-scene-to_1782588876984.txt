Fix forward and back transport buttons to jump scene-to-scene.

Problem:
The forward and back transport buttons should jump from scene to scene.

Right now they are not behaving like scene navigation.

Do not change:
- play/pause
- scrub bar
- timeline waveform
- captions
- effects
- overlays
- watermark
- aspect ratio icon
- fit mode icon
- fullscreen
- Auto PiP
- Export Doctor
- payments or credits

Only fix:
- forward scene button
- back scene button
- scene jump behavior
- active scene sync

Requirements:

1. Back button should jump to the previous scene.

Behavior:
- If current time is more than 1 second into the current scene, jump to the start of the current scene first.
- If current time is already near the start of the current scene, jump to the start of the previous scene.
- If already on scene 1, jump to 0:00.

2. Forward button should jump to the next scene.

Behavior:
- Jump to the start time of the next scene.
- If already on the last scene, jump to the end or stay at last scene start.

3. Scene jumps must use the timeline scene order.

Use:
- dragged/saved timeline order
- added clips
- deleted clips removed
- trimmed clip durations
- split clips if they exist

Do not use the old original scene order.

4. When jumping scene-to-scene, update everything:

- master player currentTime
- project audio currentTime
- timeline playhead
- audio waveform playhead
- active scene highlight
- active clip highlight
- active caption
- active effects
- active overlays
- transition state
- timecode display

5. Rename buttons clearly:

Back Scene
Forward Scene

or use icons with tooltips:
Previous Scene
Next Scene

6. Keep separate rewind/fast-forward seconds buttons.

Do not replace:
- Rewind 5 seconds
- Fast-forward 5 seconds

The scene buttons and second-jump buttons should be different controls.

7. Keyboard shortcuts:

Shift + Left Arrow = Previous Scene
Shift + Right Arrow = Next Scene

Left Arrow = rewind 5 seconds
Right Arrow = fast-forward 5 seconds

8. Add scene jump debug inside collapsed debug only:

Scene Jump Debug:
- current time
- active scene index
- active scene start
- previous scene start
- next scene start
- timeline order source
- scene jump synced: yes/no

Final goal:
The forward/back scene transport buttons should jump cleanly from scene to scene using the real timeline order, while the regular rewind/fast-forward buttons still jump by seconds.