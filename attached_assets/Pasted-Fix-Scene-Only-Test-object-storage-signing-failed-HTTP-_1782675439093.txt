Fix Scene Only Test object storage signing failed HTTP 500.

Problem:
Smart Lip Sync Doctor Scene Only Test fails with:

Object storage signing failed: HTTP 500
bucket: replit-objstore-e8e339ee-2897-443d-abd7-407651f0e97f
path: lip-sync-tests/7850b66e-4734-4754-9807-f94fd5699de5.mp4

This is not a lip sync timing problem.
The render test is failing because the app cannot create a playable/download URL for the rendered mp4.

Do not change:

* lip sync offset
* Sync.so result
* audio section
* video offset logic
* captions
* effects
* overlays
* watermark
* payments
* credits

Only fix:

* Scene Only Test storage upload/signing
* result URL creation
* fallback download route

Requirements:

1. After Scene Only Test renders, verify:

* local output file exists
* local output file size is greater than 100KB
* ffprobe confirms valid mp4
* upload started
* upload finished
* storage path exists

2. Fix object storage signing.

Show debug:

* bucket name
* object path
* upload success: yes/no
* sign request started: yes/no
* sign request finished: yes/no
* signed URL created: yes/no
* last storage error

3. If Replit object storage signing fails with HTTP 500, do not fail the test.

Add fallback:

* serve the file through a backend download route
* example route: /api/lip-sync-tests/:testId/download
* return that URL as the download/play URL

4. The Scene Only Test result must show:

* Play Test
* Download Test
* result URL
* storage method used: signed URL or backend download fallback

5. Do not only show storage error if the rendered file exists.

If render succeeded but signing failed, show:
Render succeeded, storage signing failed, using fallback download route.

6. Add Scene Only Test Result panel:

* render succeeded
* output valid
* upload succeeded
* signed URL succeeded
* fallback route created
* final playable URL
* last error

Final goal:
Scene Only Test should always give me a playable/downloadable mp4 if the render succeeded, even if object storage signing fails.
