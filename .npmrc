auto-install-peers=false
strict-peer-dependencies=false
# Allow dependency build scripts in non-interactive Docker/CI builds.
# (pnpm 12 blocks unapproved postinstall scripts by default; the lockfile
# is frozen so only locked dependencies are installed.)
dangerouslyAllowAllBuilds=true
