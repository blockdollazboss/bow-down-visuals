{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "sh ./scripts/dev.sh",
    "dev:api": "PORT=${API_PORT:-8080} pnpm --filter @workspace/api-server run dev",
    "dev:web": "PORT=${WEB_PORT:-19157} BASE_PATH=${BASE_PATH:-/} API_ORIGIN=${API_ORIGIN:-http://127.0.0.1:8080} pnpm --filter @workspace/bow-down-visuals run dev",
    "verify:audio-export:deployment": "bash ./scripts/verify-audio-export-deployment.sh",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.8",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "stripe": "^22.6.2"
  }
}
